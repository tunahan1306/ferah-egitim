export default {
    plugins: {
        "@tailwindcss/postcss": {},
        autoprefixer: {
            overrideBrowserslist: ['> 0.5%', 'last 2 versions', 'not dead', 'not op_mini all']
        }
    }
}